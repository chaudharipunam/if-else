#include <stdio.h>
int main()
{
	int p,r,t,si;
	printf("enter the no p:");
	scanf("%d",&p);
	printf("enter the no r:");
	scanf("%d",&r);
	printf("enter the no t:");
	scanf("%d",&t);
	si=(p*r*t)/100;
	printf("%d",si);
	return 0;
}
