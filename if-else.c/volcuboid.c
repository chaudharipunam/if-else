#include <stdio.h>
int main()
{
	int l,w,h,vc;
	printf("enter the no.of l: ");
	scanf("%d",&l);
	printf("enter the no.of w: ");
	scanf("%d",&w);
	printf("enter the no.of h: ");
	scanf("%d",&h);
	vc=l*w*h;
	printf("%d",vc);
	return 0;
}

